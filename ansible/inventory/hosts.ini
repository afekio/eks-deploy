# The whole deployment is orchestrated from the control node (your machine),
# which already has terraform / helm / kubectl / aws configured. Everything
# therefore runs against localhost with a local connection.
[local]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3
